{
  "name": "@wowsp/holo",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "Shared holographic replay HUD components (scorebar / clock / minimap / labels) used by the desktop app and the marketing site. Also hosts shared frontend composables (useImage).",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "peerDependencies": {
    "three": "^0.*",
    "vue": "^*"
  }
}
